Parsing Errors
Expansion
Error codes & last_pid $?
If no env
Signal handling
Make is_builtin func
built-ins : cd, export, unset, exit (clear hisory), env (print env vars)
Why clear_history ?
Look at the readline functions
Handle input
Here-doc mode
Check for commands before executing them : echo hello | notexist -d l  --> notexist command not found
Redirect ouput for built-ins : echo hello > hello.txt --> no output

Anton
============

Tom
===========
Working on is_builtin() function
Replace printf with dprintf -> STDERR
Make a t_cmd_list struct {
	t_command *head;
	t_command *curr_cmd;
	int		remaining;
} ?
